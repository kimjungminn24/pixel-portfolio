This is where the project description goes. The markdown file that `aboutFile` points to in `data/portfolio.json` is rendered here. If it is short, put the text straight into the `about` field instead.

## Supported syntax

- Headings `#` `##` `###`
- **bold**, *italic*, ~~strikethrough~~, ==highlight==, `code`
- Lists, numbered lists, quotes
- Tables, rules, images, links

| Item | Value |
| --- | ---: |
| Pages | 4 |
| Dependencies | 0 |

```js
// code blocks work too
const theme = localStorage.getItem('pixel-portfolio:theme');
```

> This is how a quote looks.
